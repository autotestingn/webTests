package pages;

import org.openqa.selenium.WebDriver;

public class UserPage extends BasePage<UserPage> {
    public UserPage(WebDriver driver, String pathName) {
        super(driver, pathName);
    }
}
